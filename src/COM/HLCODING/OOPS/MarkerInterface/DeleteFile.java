package COM.HLCODING.OOPS.MarkerInterface;

public interface DeleteFile { // it is a interface
    // interface have only abstract method and we cannot create the object of interface
    //here we will write the delete file code
    //we can also create the class and extends it but another class can not extends
// the class that's the reson we create interface and implement it into class

    /*default int hashCode()
    {

    }*/

    // we cannot add def hashcode method because hashcode method is already defined in object class
}


